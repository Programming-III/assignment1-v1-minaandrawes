#ifndef MAMMAL_H
#define MAMMAL_H

#include "Animal.h"

class mammal: public animal(
private:
   string furColor;
public:
   bird();
   bird(string fc);
   ~bird();
   string getFurColor();
   void setFurColor(string f);
   );

#endif
